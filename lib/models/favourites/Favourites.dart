class Favourites {
  int pk;
  String username;
  String full_name;
  String image_url;
  String description;

  Favourites(this.pk, this.username, this.full_name, this.image_url, this.description);
}
