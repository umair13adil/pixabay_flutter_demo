
class Downloaded{

  String username;
  int userid;
  String image_url;
  String video_url;
  bool is_video;

  Downloaded(this.username, this.userid, this.image_url, this.video_url,
      this.is_video);

}